Read both Android apps, which are totally different, but I have coordinated features between them. Read the entire content of both Android apps: one is the two FA and the other is the vaults. You will find every file related to them. Make sure to read each Android file perfectly so you can do the best without any confusion. Then read the last few commits of both of them and read this pasted chat, then continue from there as I have told. Ensure that whatever you do you are 100% confident it is correct and everything is perfect. And continue with this last i ask Is it possible that when I select any text, in those Android options, can you add one more option to directly open in the Chrome incognito mode? It means it will open the incognito mode, and then it will open that same URL that I have selected. Means when only the URL is selected in that. Means when the URL is selected, first of all, you have to just find does it it is a URL? If it is a URL, then you just show an option to directly open in the Chrome incognito mode and make sure that you do not expose that URL anywhere else other than the Chrome incognito mode, okay? Only it will open in the Chrome's incognito mode. So make sure that 100% it's open that URL in the incognito mode

I’ll trace the coordinated sharing flow across both Android apps, review the recent history and the attached handoff notes, then implement the incognito URL action at the point where Android text selections are handled. I’ll also verify the intent behavior and release build path so the URL is passed only to Chrome’s incognito tab.

The handoff confirms the selection toolbar is in the VoltShare viewer, and the requested action belongs there rather than in the 2FA vault logic. I’m now locating the exact text-rendering and activity/intent boundaries in both codebases, plus the recent changes that established the current viewer behavior, so the feature is added without changing transfer or vault semantics.

The current viewer uses Compose selection, which provides Android’s standard actions but no supported way to add a conditional custom action. I’m replacing only the viewer’s selectable text surface with a native selectable TextView, preserving the existing full-copy and file-reading behavior while adding a selection action that appears only for a strictly selected HTTP/HTTPS URL and launches an explicit Chrome intent with Chrome’s incognito extra. No fallback browser, clipboard copy, logging, or URL-bearing toast will be used.